/**
 * Backward-compatible re-export.
 * Prefer: import { createExperimentManager } from './experiments/index.js'
 *       or import { createExperimentManager } from './experiments'
 */
export { createExperimentManager, STATION_EXPERIMENTS, STATION_MODULES } from './experiments/index.js';
