/**
 * Backward-compatible application export for legacy consumers.
 * The actual whiteboard source now lives in apps/WhiteboardApp.tsx.
 */
export { WhiteboardApp as default, WhiteboardApp } from './apps/WhiteboardApp';
